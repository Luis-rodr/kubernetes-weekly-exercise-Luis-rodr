apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "laravel-luis.fullname" . }}-test-connection"
  labels:
    {{- include "laravel-luis.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "laravel-luis.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
