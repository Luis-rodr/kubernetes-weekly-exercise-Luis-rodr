apiVersion: v1
kind: Service
metadata:
  name: {{ include "laravel-luis.fullname" . }}
  labels:
    {{- include "laravel-luis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "laravel-luis.selectorLabels" . | nindent 4 }}
